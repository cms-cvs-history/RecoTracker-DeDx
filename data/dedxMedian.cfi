module dedxMedian = DeDxEstimatorProducer {
  string   estimator                  = "median"

  bool     BuildFromTrajectory        = true
  InputTag TrajectoryStateOnDetInfo   = Tsodi           #Used only if BuildFromTrajectory=False
  InputTag Track                      = generalTracks   #Used only if BuildFromTrajectory=True
  InputTag trajectoryTrackAssociation = generalTracks   #Used only if BuildFromTrajectory=True
}
